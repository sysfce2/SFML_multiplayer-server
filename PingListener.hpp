#pragma once
#include <unordered_map>
#include "IEventListener.hpp"

class PingListener : public IEventListener {
//private:
//	std::unordered_map<>
//private:
//	void handle(EPacketReceived e) override;
};